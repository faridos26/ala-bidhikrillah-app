# لحظة سكينة — سجل التحديثات v1.0 → v1.1

## 🔴 إصلاحات حرجة
- **تفعيل مسار الأزمات `SAFETY_CRISIS`** الذي كان مُعرَّفًا في `settings` بلا أي تنفيذ فعلي:
  - تصنيف جديد `SAFETY_CRISIS` (`is_safety_route = 1`)
  - 12 كلمة كشف لنوايا إيذاء النفس/الانتحار
  - محتوى من نوع جديد `SAFETY_ACTION`: رسالة توجيه فورية لطلب مساعدة بشرية/مختصة، + آية دعم (يوسف 12:87)
  - ⚠️ رسالة `SAFETY_ACTION` لا تتضمن رقم طوارئ محددًا عمدًا (لتفادي رقم خاطئ حسب الدولة) — يجب على الفريق ربطها برقم الخط الساخن المحلي حسب دولة كل مستخدم قبل الإطلاق. راجع `settings.safety_hotline`.

## 🟠 تحسينات المخطط (Schema)
- إضافة `FOREIGN KEY` من `keywords.category_code` و`content.category_code` إلى `categories.code`
  - ⚠️ **مهم**: SQLite يعطّل تطبيق `FOREIGN KEY` افتراضيًا لكل اتصال جديد. يجب تنفيذ `PRAGMA foreign_keys = ON;` في بداية كل اتصال بقاعدة البيانات من كود التطبيق، وإلا فالقيد موجود شكليًا فقط ولن يمنع أي خطأ.
- إضافة `UNIQUE(category_code, keyword)` على `keywords` لمنع التكرار الصامت
- إضافة `CHECK` على `content.type` (`QURAN`/`HADITH`/`SAFETY_ACTION`) و`content.status` (`VERIFIED`/`PENDING_REVIEW`)
- إضافة فهارس `idx_keywords_category` و`idx_content_category` لتسريع الاستعلام حسب التصنيف
- إضافة أعمدة تتبع: `content.added_by`, `content.created_at`
- إضافة عمود `keywords.match_type` (قيمة افتراضية `word_boundary`) — **يوثّق أن مطابقة الكلمات يجب أن تتم بحدود الكلمة (`\b...\b`) وليس بالاحتواء الجزئي (substring/`LIKE %..%`)**، لحل تداخلات كانت موجودة مثل "قرار" داخل "استقرار" و"أم" داخل "أمل" دون حذف أي كلمة مفيدة.

## 🟡 إثراء المحتوى
- كل تصنيف كان يحتوي عنصرًا واحدًا فقط (15 تصنيفًا) أصبح يحتوي عنصرين على الأقل — لتفادي تكرار نفس النص لنفس المستخدم في كل زيارة.
- **جميع العناصر المُضافة وُسمت `status = PENDING_REVIEW` و`added_by = audit_addition_2026_09_05`**، بخلاف المحتوى الأصلي الذي بقي `VERIFIED` و`original_seed` — حتى يبقى الفرق واضحًا ولا يُنشر شيء جديد دون مراجعة سريعة من فريقكم لتأكيد رقم الحديث الدقيق حسب الطبعة المعتمدة (بعض الأحاديث المضافة وُثّقت بلا رقم محدد عمدًا لعدم الجزم برقم قد يختلف بين الطبعات).
- ملاحظة: النص القرآني والمرجع (سورة:آية) لكل الإضافات القرآنية بمستوى ثقة عالٍ، لأنها آيات مشهورة وثابتة الترقيم.

## ملخص الأرقام
| | v1.0 | v1.1 |
|---|---|---|
| التصنيفات | 19 | 20 (+SAFETY_CRISIS) |
| الكلمات المفتاحية | 98 | 110 |
| عناصر المحتوى | 23 | 40 (23 VERIFIED + 17 PENDING_REVIEW) |

## ما زال يحتاج قرار فريقكم (وليس تدخلًا نصيًا مني)
1. تأكيد أرقام الأحاديث المُعلَّمة "يُرجى تأكيد رقم الحديث" حسب طبعتكم المعتمدة، ثم تغيير حالتها إلى `VERIFIED`
2. ربط `SAFETY_ACTION` برقم هاتف/خط ساخن محلي فعلي حسب دولة المستخدم
3. مراجعة صياغة رسالة `SAFETY_ACTION` من قِبل مختص نفسي/قانوني قبل الإطلاق (نص تشغيلي وليس شرعيًا)

---

# v1.1 → v1.2 — ميزة التبرع الاختياري

## جدولان جديدان
- **`donation_settings`**: نصوص الرسالة (عنوان/نص/زر)، وقواعد التوقيت:
  - `trigger_min_content_views = 3` — لا تظهر إلا بعد استفادة فعلية متكررة، لا من أول استخدام
  - `cooldown_days_after_dismiss = 14` — لا تُعاد على من أغلقها قبل مرور أسبوعين
  - `excluded_route_rule = NEVER_SHOW_WHEN_LAST_CATEGORY_IS_SAFETY_ROUTE` — **قاعدة إلزامية**: لا تظهر رسالة التبرع أبدًا إذا كان آخر تصنيف عُرض على المستخدم `is_safety_route = 1` (أي `SAFETY_CRISIS` حاليًا، وأي مسار أمان يُضاف مستقبلًا تلقائيًا دون تعديل كود إضافي)
  - `enabled = false` افتراضيًا — لا تُفعّل الميزة تلقائيًا
- **`donation_methods`**: 4 وسائل (هبة إلكترونية، تحويل بنكي، دفع محلي، دولي لاحقًا) — **كلها `is_active = 0`** لأنها لا تحتوي تفاصيل دفع حقيقية بعد. يجب تعبئة عمود `details` ببيانات حقيقية وتفعيل `is_active = 1` لكل وسيلة قبل عرضها للمستخدم، لتفادي زر يقود لصفحة فارغة.

## ما زال يحتاج قرار فريقكم
4. تعبئة تفاصيل وسائل الدفع الحقيقية وتفعيلها واحدة واحدة
5. مراجعة قانونية لقبول التبرعات (تسجيل كجهة خيرية) حسب دولة الإطلاق
6. تفعيل `DONATE` فقط بعد جاهزية وسيلة دفع واحدة على الأقل

---

# v1.5 → v1.6 — تفعيل التبرع كـ"اختبار اهتمام" فقط (بلا تحصيل مال)

## الفكرة
قبل الالتزام بالخطوة القانونية (تسجيل ككيان يقبل تبرعات)، نقيس الطلب الفعلي أولاً. الزر يظهر بشكله الكامل، لكن الضغط عليه **لا يفتح أي صفحة دفع** — فقط يزيد عدّاد اهتمام (`countapi.xyz`، خدمة عدّاد مجانية بلا تسجيل ولا بيانات شخصية) ويعرض رسالة شكر.

## لماذا هذا آمن قانونيًا وسابقه لم يكن
لا مال يتحرك، لا بيانات دفع تُجمع، لا وعد بخدمة مقابل مبلغ — فقط قياس نية. `donation_methods` كلها بقيت `is_active = 0` كما هي، فلا طريق فعلي لإتمام أي تبرع حتى لو أراد أحد ذلك.

## للفريق
- تابعوا رقم `sakina-app/donate-interest` على `countapi.xyz` (رابط مباشر: `https://api.countapi.xyz/get/sakina-app/donate-interest`) لمعرفة عدد المهتمين
- `settings.donation_mode = 'INTEREST_TEST_ONLY_NO_MONEY_COLLECTED'` يوثّق أن هذه مرحلة اختبار لا إطلاق فعلي
- إذا أعطى الرقم إشارة كافية، عندها فقط تنتقلون لتفعيل وسائل الدفع + المراجعة القانونية (البندان 4 و5 أعلاه)

---

# v1.2 → v1.3 — تعميم "رسائل التفاعل" (مشاركة/تقييم/تبرع)

## القرار
بدل مسار مخصص للتبرع فقط، أصبح هناك نظام عام واحد `engagement_prompts` يدعم 3 أنواع بنفس منطق التوقيت والاستبعاد الأمني، فتُفعِّلون/تُعطّلون كل نوع من `enabled` بلا أي تعديل على قاعدة البيانات لاحقًا:

| النوع | الحالة الآن | السبب |
|---|---|---|
| `SHARE` (📤 شارك التطبيق) | **مفعّل** | بلا أي بعد مالي أو قانوني، يبني قاعدة المستخدمين وهي أساس أي خطوة تالية |
| `RATE` (⭐ قيّم التطبيق) | معطّل | ينتظر نشر فعلي على المتاجر وروابط حقيقية (`store_url_ios`/`store_url_android` فارغة الآن) |
| `DONATE` (🤍 ساهم في استمرار الخير) | معطّل | يحتاج جاهزية وسيلة دفع + مراجعة قانونية، كما قرر الفريق |

## الجداول
- **`engagement_prompts`**: صف واحد لكل نوع — `enabled`, نصوص العرض, `trigger_min_content_views`, `cooldown_days_after_dismiss`, و**نفس قاعدة الاستبعاد الأمنية الإلزامية** `NEVER_SHOW_WHEN_LAST_CATEGORY_IS_SAFETY_ROUTE` مطبّقة على الأنواع الثلاثة بلا استثناء — لا تظهر أي رسالة تفاعل (حتى المشاركة) مباشرة بعد مسار أزمة.
- **`engagement_prompt_meta`**: حقول إضافية خاصة بكل نوع (نص/رابط المشاركة لـ`SHARE`، روابط المتجر لـ`RATE`) بنمط مفتاح/قيمة، لأن كل نوع يحتاج بيانات مختلفة جذريًا ولا معنى لتكديسها كأعمدة فارغة في جدول واحد.
- **`donation_methods`**: بقي كما هو لكنه الآن مربوط رسميًا بـ`prompt_type = 'DONATE'` عبر `FOREIGN KEY`، بدل اعتماد ضمني غير موثّق.
- حُذف الجدول المخصص `donation_settings` بعد ترحيل كل قيمه.

## ملاحظة عن مسؤولية "المشاركة" القانونية
نص `SHARE` مقصود ألا يتضمن أي وعد علاجي/طبي (لا "يعالج القلق" ولا "يشفي الحزن") — فقط وصف محايد "يُعين على لحظات الحزن والقلق والفرح"، لتفادي أي التباس بادعاء طبي قد يُحمّل التطبيق مسؤولية لاحقًا. يُنصح الإبقاء على هذا النمط في أي نص تسويقي مستقبلي للتطبيق.

## ما تبقى لتفعيل `SHARE` فعليًا
- تعبئة `engagement_prompt_meta.share_url` برابط حقيقي (متجر/موقع) بدل `PLACEHOLDER`
